version: '3.7'
services:
  postgres:
    image: postgres
    restart: 'always'
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
